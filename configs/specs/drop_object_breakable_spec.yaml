# Specification for resolving drop items from breakable objects.

sources:
  - name: "master_dropitem_for_breakable_object"
    type: "local_file"
    path_type: "survival"
    path: "master_dropitem_for_breakable_object.dat"
    decoder: "decode_survival_dat"
    strategy: "lookup"
    key: "dropId"

primary_source: "master_dropitem_for_breakable_object"

output_structure:
  id: { from: "master_dropitem_for_breakable_object", field: "dropId" }
  drop_group: { from: "master_dropitem_for_breakable_object", field: "dropGroup" }
  item_id: { from: "master_dropitem_for_breakable_object", field: "itemId" }
  drop_min_amount: { from: "master_dropitem_for_breakable_object", field: "dropMinAmount" }
  drop_max_amount: { from: "master_dropitem_for_breakable_object", field: "dropMaxAmount" }
  weight: { from: "master_dropitem_for_breakable_object", field: "weight" }